name: Triggers
on:
  push:
    branches:
      - 'main'
  # workflow_dispatch
  # [push] # Tiggers the workflow on every push to the repository

jobs:
  # job name
  trigger:
    runs-on: ubuntu-latest  # The type of machine to run the job on
    steps:
      # - name: Manual dispatch
      #   run: echo "manually dispatched"
      - name: feature branch push
        run: echo "feature branch push"